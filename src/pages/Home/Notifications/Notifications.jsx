import React from "react";
import { observer } from "mobx-react";

import "./Notifications.less";

export const Notifications = observer(() => {
  return <div className="notifications__container">Notifications</div>;
});
